import styled from "@emotion/styled";
import { Box } from "@mui/material";

export const TagBoxStyled = styled(Box)({
  fontWeight: 700,
  fontStyle: "normal",
  borderRadius: "2rem",
  display: "inline-flex",
  padding: "0.5rem 1.2rem",
});
